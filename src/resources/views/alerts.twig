{% set alert_types = ['success','info','warning','error'] %}

{% for alert_type in alert_types %}
    {% if session_has(alert_type) %}
        {% include 'cornerstone::alerts.' ~ alert_type with { message: session_pull(alert_type) } %}
    {% endif %}
{% endfor %}
